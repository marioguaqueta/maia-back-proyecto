# ==============================================
# Streamlit Frontend - Environment Variables
# ==============================================

# API Configuration
# -----------------
API_BASE_URL=http://localhost:8000
ADMIN_EMAIL=info@grupo12.yolomodel.com

# Help & Documentation URLs
# --------------------------
# URL for video tutorials and explanations
EXPLAIN_VIDEO_URL=https://example.com/tutorials
# URL for online documentation
DOCS_URL=https://example.com/docs

# Model Availability
# ------------------
# Enable/disable HerdNet model in the UI (true/false)
# Set to false to hide HerdNet option and show only YOLOv11
ENABLE_HERDNET=true

# ==============================================
# YOLOv11 UI Configuration
# ==============================================

# Confidence Threshold Slider
# ----------------------------
YOLO_CONF_MIN=0.1
YOLO_CONF_MAX=0.9
YOLO_CONF_DEFAULT=0.25
YOLO_CONF_STEP=0.05

# IOU Threshold Slider
# --------------------
YOLO_IOU_MIN=0.1
YOLO_IOU_MAX=0.9
YOLO_IOU_DEFAULT=0.45
YOLO_IOU_STEP=0.05

# Image Size Options
# ------------------
# Comma-separated list of image sizes
YOLO_IMG_SIZES=416,480,640,800,960,1280,2560,5120,10240
# Index of default selection (0-based)
YOLO_IMG_SIZE_DEFAULT_INDEX=2

# ==============================================
# HerdNet UI Configuration
# ==============================================

# Patch Size Options
# ------------------
# Comma-separated list of patch sizes
HERDNET_PATCH_SIZES=384,512,768,1024,2048,4096,8192,16384
# Index of default selection (0-based)
HERDNET_PATCH_SIZE_DEFAULT_INDEX=1

# Rotation Options
# ----------------
# Comma-separated list of rotation steps (0, 1, 2, 3 = 0°, 90°, 180°, 270°)
HERDNET_ROTATION_OPTIONS=0,1,2,3
# Index of default selection (0-based)
HERDNET_ROTATION_DEFAULT_INDEX=0

# Overlap Slider
# --------------
HERDNET_OVERLAP_MIN=0
HERDNET_OVERLAP_MAX=300
HERDNET_OVERLAP_DEFAULT=160
HERDNET_OVERLAP_STEP=16

# Thumbnail Size Slider
# ---------------------
HERDNET_THUMBNAIL_MIN=128
HERDNET_THUMBNAIL_MAX=512
HERDNET_THUMBNAIL_DEFAULT=256
HERDNET_THUMBNAIL_STEP=32

# ==============================================
# Image Zoom Configuration with Plotly
# ==============================================

# Plotly provides interactive zoom/pan controls with optimized performance
# for large images to avoid WebSocket overload

# Maximum Display Dimension
# -------------------------
# Images larger than this (width or height) will be downsampled for display
# This prevents WebSocket errors and browser freezing
# Default: 1500 pixels
PLOTLY_MAX_DIMENSION=1500

# Fallback Threshold
# ------------------
# Images larger than this will use static st.image instead of Plotly
# This is for extremely large images that would crash WebSocket even when downsampled
# Default: 3000 pixels
PLOTLY_FALLBACK_THRESHOLD=3000

# How it works:
# - Images < 1500px: Display at full resolution with Plotly zoom
# - Images 1500-3000px: Downsample to 1500px max, then display with Plotly zoom
# - Images > 3000px: Show warning and use static st.image viewer
#
# Benefits:
# - Prevents WebSocket crashes and browser freezing
# - Maintains interactive zoom for most images
# - Gracefully handles very large images
# - Users can still download full-resolution originals

# Notes:
# ------
# 1. Copy this file to .env in the .streamlit directory
# 2. Or set these as environment variables when running Streamlit
# 3. All numeric values should be provided without quotes
# 4. List values should be comma-separated without spaces
# 5. These values control the UI parameters shown to users

